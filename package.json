{
  "name": "wedding-invitation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "prepare:envelope": "node scripts/prepare-envelope-assets.mjs"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^6.0.0"
  }
}
